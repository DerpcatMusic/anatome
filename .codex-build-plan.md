Build/check repair plan

1. Capture current verification failures in `.check-run-check.txt`.
2. Fix broad configuration/import failures first: test runner types and Convex `.ts` import extensions.
3. Fix schema/type drift around video tag tables and generated Convex types.
4. Fix LiveKit context export and component prop errors.
5. Fix remaining Svelte/component type errors, rerunning `bun run check` until clean.
6. Run `bun run build`, save output, and fix any build-only failures.
