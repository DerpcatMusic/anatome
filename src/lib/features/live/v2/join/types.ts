/** v2 join types — shared Convex contract (see `join-token.ts`). */
export type {
  JoinAccessSnapshot,
  JoinContextSnapshot,
  JoinInfo,
  JoinTokenPhase,
  JoinTokenSnapshot,
} from "../../join-token";

export type { RoomStatus, ConnectionState } from "../../types";
