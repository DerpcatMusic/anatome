/**
 * Client feature flags. Keep in sync with `convex/lib/featureFlags.ts`.
 */
export const SUBSCRIPTIONS_ENABLED = false;

/** Credit checkout — off until payment flow ships. */
export const CREDITS_PURCHASE_ENABLED = false;
